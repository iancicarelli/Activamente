# Dependencias solo para desarrollo/tests (NO se instalan en la imagen; el
# Dockerfile instala requirements.txt). Instalar con:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.6.9

# Auditoría de seguridad (scripts/test.sh audit)
bandit==1.8.3
pip-audit==2.9.0
