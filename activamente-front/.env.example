# Copiar a `.env`. URL del backend FastAPI vista desde el celular (misma WiFi que tu PC).
# Sin esta variable, services/config.ts usa 10.0.2.2:8420 (emulador Android) o localhost.
# Tras cambiarla: npx expo start -c
EXPO_PUBLIC_API_URL=http://192.168.1.10:8420
